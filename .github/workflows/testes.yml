name: CI Testes

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: shortener
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install deps
        run: npm ci

      - name: Install psql client
        run: |
          sudo apt update
          sudo apt install -y postgresql-client

      - name: Aguardar Postgres subir
        run: |
          for i in {1..10}; do
            PGPASSWORD=postgres psql -h localhost -U postgres -d shortener -c "SELECT 1" && break
            echo "Postgres ainda não está pronto... tentando novamente"
            sleep 3
          done

      - name: Criar tabela URLs
        run: |
          PGPASSWORD=postgres psql -h localhost -U postgres -d shortener -c "
            CREATE TABLE IF NOT EXISTS urls (
              code TEXT PRIMARY KEY,
              url TEXT NOT NULL
            );
          "

      - name: Debug tabelas
        run: |
          PGPASSWORD=postgres psql -h localhost -U postgres -d shortener -c "\dt"

      - name: Permitir Jest
        run: chmod +x node_modules/.bin/jest || true

      - name: Rodar testes
        env:
          PGHOST: localhost
          PGUSER: postgres
          PGPASSWORD: postgres
          PGDATABASE: shortener
          PGPORT: 5432
        run: npm test
